# 2026-05-25 작업 요약 (5개 요구사항 반영)

작업 경로: `/home/leetaeho/ai_07_high/deskad_keyboard_demo`

## 1. 렌더링 사실감 개선

- `backend/main.py /viewer` 와 `streamlit_app.py render_model_viewer`
  - `@google/model-viewer@4.0.0` 으로 버전 고정
  - `environment-image="neutral"`, `tone-mapping="aces"` 추가 (PBR/HDR 톤매핑)
  - `shadow-intensity 0.8 → 1.4`, `shadow-softness="0.85"`, `exposure 0.72 → 1.05`
  - 배경을 radial-gradient 로 변경
- `backend/renderer.py GlbBuilder.add_material`
  - `alpha`, `alphaMode`, `emissive`, `emissive_strength` 지원 (mist/디스플레이/광원의 발광 표현)
  - `KHR_materials_emissive_strength` extension 자동 등록
- `add_torus_y`, `add_sphere` 신규 primitive 추가 (헤드폰 밴드, 식물 잎, 마우스 본체 곡면)
- 데스크 표면을 wood + edge 두 머티리얼로 분리, 모니터를 chassis/bezel/display 3층 구조로 분리

## 2. 샘플 위치 정합성 수정

- `backend/renderer.py DeskPlacer` 클래스 신설
  - 책상 폭/깊이 안에서 각 에셋의 bbox 를 추적하고 충돌 시 다음 후보 슬롯으로 fallback
  - 후보가 모두 실패하면 해당 에셋은 skip (메타에 `placed_items` 로 기록)
- 마우스, 데스크매트, 키보드 위치를 책상 폭에 맞춰 동적 계산
  - 좁은 책상(100cm)에서 마우스가 책상 밖으로 나가지 않음
  - 모니터 base + 모니터암 + speakers 등이 같은 후면 zone 에서 겹치지 않음
- 데스크매트가 키보드 footprint 기준으로 자동 리사이즈

## 3. 키보드 상세 커스텀 (PCB/스위치/보강판/키캡)

- `backend/renderer.py _add_keyboard_detailed` 신규
  - 케이스 (상/하 하우징, 안쪽 캐비티)
  - PCB + gold trace
  - plate
  - switch housing + top + stem (color-coded)
  - keycap + legend (작은 키에만)
- `backend/main.py KeyboardRenderRequest` 에 다음 필드 추가
  - `case_finish` (anodized/matte/polycarbonate/wood)
  - `plate_material` (aluminum/brass/pom/fr4/carbon/polycarbonate)
  - `pcb_color` (black/red/blue/green/white)
  - `switch_stem` (red/yellow/brown/blue/clear/silent_red/tactile_purple/linear_black)
  - `show_internals` (분해도처럼 두께 분리)
- `streamlit_app.py` 2단계(도면) 에 키보드 상세 커스텀 expander 추가, 3단계(셋업)에 마감/보강판/스위치 요약 caption

## 4. 데스크테리어 카탈로그 확장

- `data/desk_assets.json` 12 → 22 개로 확장
- 추가된 에셋
  - `monitor_light_bar` (모니터 라이트 바)
  - `digital_clock` (디지털 시계)
  - `aroma_diffuser` (아로마 디퓨저, mist 알파 블렌딩)
  - `wireless_charger` (Qi 무선 충전 패드)
  - `pen_holder` (펜 홀더 + 펜 4종)
  - `book_stack` (책 3권 스택)
  - `humidifier` (가습기 + mist)
  - `photo_frame` (기울어진 사진 액자)
  - `usb_hub` (4포트 USB 허브)
  - `mouse_pad_round` (라운드 마우스패드)
- 각 항목에 `dimensions_cm`, `external_candidates`(Poly Pizza / Sketchfab) 기록
- `monitor` 항목에 24/27/32인치 dimensions 모두 기록
- 모니터암 `style: single | double_joint` 지원

## 5. 로컬 모델 기반 광고 포스터/문구

- `backend/ai.py` 전면 개편
  - JSON 강제 응답 (`response_format=json_object`, fenced/braces 파서)
  - `spec_bullets` 필드 추가
  - `_local_copy` URL 정규화 (`/v1` 유무, `/chat/completions` 직지정 모두 허용)
  - tone hint 4종 추가
- 포스터 템플릿 4종 추가
  - `minimal_card` (기존, 다듬음)
  - `grid_three` (3컷 그리드)
  - `feature_focus` (좌측 hero + 우측 SPEC bullet)
  - `promo_banner` (16:9 광고 배너)
- 로컬 이미지 endpoint (`LOCAL_IMAGE_ENDPOINT`) 응답에서 이미지 추출 정규화
  - `image_base64` / `image_b64` / `image` / `data[0].b64_json` / `data[0].url` / 최상위 `url` 모두 지원
  - 추출 성공 시 SVG `<image href="data:image/png;base64,...">` 로 포스터에 직접 합성
  - 실패 시 SVG 일러스트 fallback
- Streamlit 4단계에 포스터 템플릿 선택, AI 상태(OpenAI/Local LLM/Local Image) 아이콘 표시
- 포스터 탭에 템플릿 라벨, 합성 여부 배지 표시
  - API 응답 자체는 `poster_template`, `image_embedded` 를 반환하고, Streamlit 이 `poster_template` 을 화면 라벨로 매핑

## 검증 결과

```text
py_compile: backend/main.py renderer.py ai.py streamlit_app.py → OK
/health: 200, openai_api_key=missing, local_llm_base_url=missing, local_image_endpoint=missing
/viewer: 200, model-viewer 4.0.0 / neutral environment / ACES tone mapping 확인
/assets/desk: 22개 에셋, 기본 5개, dimensions_cm / external_candidates 필드 확인
/render/desk-setup (160x80, 65%, monitor_arm double_joint, brass plate, silent_red, 11종 에셋): 200
  - GLB 생성, 32인치 모니터 panel 74x43cm, brass plate, silent_red 반영
  - placed_items 10개 반환. placed_items 는 요청 assets 전체가 아니라 DeskPlacer 가 충돌 관리를 위해 reserve 한 항목 목록임
  - monitor / monitor_light_bar 처럼 고정 배치되는 일부 항목은 placed_items 에 포함되지 않을 수 있음
/ai/poster (feature_focus template): 200, poster_template=feature_focus, image_embedded=false, spec_bullets 3개 반환
http://127.0.0.1:8501: 200
http://34.27.86.182:8501: 200
```

## 알려진 한계

- OpenAI/Local LLM/Local Image 모두 `.env` 미설정 상태에서는 fallback 카피와 SVG 일러스트만 사용 가능
- 절차적 GLB는 PBR 머티리얼 + tone mapping 으로 사실감을 개선했으나 실사 수준은 아님 — 실제 GLB 에셋 병합은 다음 단계
- DeskPlacer 가 너무 작은 책상(100x50)에 22개 모든 에셋을 강제하면 절반 가량 skip (정상 동작)
- 현재 VM의 `sprint_high` 환경에는 Playwright/Chromium 이 설치되어 있지 않아 이번 재검증은 API/TestClient/HTTP 응답 기반으로 수행. 브라우저 회귀 검증은 Playwright 설치 후 별도 수행 필요

## 외부 GLB 라이선스 메모

`data/desk_assets.json` 각 항목의 `external_candidates` 는 모두 후보 링크일 뿐, 실제 다운로드/번들링 전 라이선스 개별 확인 필요. 상업 사용 가능 여부와 출처 표기 조건을 반드시 검토.
