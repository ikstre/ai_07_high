# DeskAd AI Studio - 다음 작업 계획 (2026-05-28 기준)

작성일: 2026-05-28
기준 브랜치: `taeho` (HEAD: `8c6b394 Merge pull request #3 from ikstre/taeho` + 미커밋 보안/모델 작업)

이 문서는 `docs/project_handoff_2026-05-28.md` 이후의 다음 작업을 우선순위와 영역(모델 연결 / UI 향상)으로 구분한다.

## 진행 기록

- 2026-05-28: `webdemo` 병합 완료 상태를 확인했다 (`c3cae82 merge: webdemo...`).
- M1 완료: `tools/systemd/comfyui.service`를 `/etc/systemd/system/comfyui.service`에 설치하고 `enabled/active` 상태로 전환했다. 기존 수동 `nohup` ComfyUI 프로세스는 종료해 8188 포트를 systemd 서비스가 단독 점유한다.
- `start.sh` preflight에 ComfyUI/Ollama 의존성 체크를 추가했고, 앱 기본 API 포트를 8000이 아닌 8010으로 정리했다.

---

## 0. P0 — `webdemo` 브랜치 병합 (가장 먼저)

### 0-1. 현재 상태

- 원격에 별도로 `webdemo` 브랜치가 존재한다. 최신 commit: `a87468d 모델링 1차 수정`
- main 대비 변경량: **13 files changed, 3,413 insertions(+), 196 deletions(-)**
- 주요 변경 파일 (`git diff --stat origin/main...origin/webdemo`):

  ```text
  backend/ai.py                                   656 +++++++
  backend/config.py                               103 +++
  backend/drawing_converter.py        (신규)        84 ++
  backend/main.py                                 296 +++
  backend/plates.py                   (신규)       122 ++
  backend/renderer.py                             1454 ++++++++++
  data/assemblies/keyboard_assembly_sample.json    32 ++
  data/drawings/keyboard_layout_plate_65.json      28 ++
  data/drawings/keyboard_layout_plate_75.json      28 ++
  data/layouts/layout_87.json                     40 ++
  data/layouts/layout_104.json                   134 ++
  streamlit_app.py                                628 +++
  .gitignore                                        4 +
  ```

### 0-2. 예상 충돌 영역

`taeho` 브랜치 (현재 작업)와 `webdemo` 브랜치가 **양쪽 다 크게 수정한 파일**이 5개라 머지 충돌이 거의 확실하다.

| 파일 | taeho 변경 (이번 세션) | webdemo 변경 | 병합 전략 |
|---|---|---|---|
| `backend/ai.py` | LLM provider 분리, image job store, sanitize, prompt 강화 | +656줄, 모델링 관련 추정 | **수동 병합 필수**. 함수 단위로 두 브랜치 의도 모두 살림 |
| `backend/main.py` | 신규 API (`/ai/image/jobs`, `/quality`, `/ai/providers` 등), Pydantic 강화, CORS allowlist | +296줄, drawing/plate endpoint 추정 | 라우터 정의를 모두 보존, Pydantic 충돌 시 더 엄격한 쪽 채택 |
| `backend/config.py` | HyperCLOVA direct, CORS env, ComfyUI/Image backend, 공용 폴더 | +103줄, 도면/plate 관련 추정 | Settings dataclass field 둘 다 흡수 |
| `backend/renderer.py` | bevel/seam/mount/keycap profile 등 디테일 | +1454줄, 본격 모델링 (assemblies/drawings) | **가장 큰 충돌 영역**. webdemo 쪽 모델링 작업을 base로 두고 taeho의 디테일 옵션을 위에 얹는 방향 추천 |
| `streamlit_app.py` | 한글 모델 비교, 이미지 작업/품질 expander | +628줄, 도면 업로드/조합 UI 추정 | 단계별 섹션 분리해 양쪽 UI 모두 포함 |

### 0-3. 권장 병합 절차

```bash
cd /home/leetaeho/ai_07_high
git fetch origin --prune

# 1) 안전 백업
git checkout taeho
git switch -c taeho-backup-2026-05-28
git switch taeho

# 2) webdemo를 끌어와 병합 시도 (rebase 대신 merge 권장 - 충돌 해소 추적이 쉬움)
git merge origin/webdemo --no-commit --no-ff

# 3) 충돌 영역별 작업
#    - 텍스트 충돌: VS Code/PyCharm 머지 도구로 수동
#    - 신규 파일(backend/drawing_converter.py, backend/plates.py, data/assemblies/*, data/drawings/*): 그대로 채택
#    - data/layouts/layout_87.json, layout_104.json: 그대로 채택 후 main.py의 layout 라우터에 반영
#    - .gitignore: 합집합 (taeho에서 강화한 시크릿 패턴 + webdemo의 추가 4줄)

# 4) 검증
conda run -n sprint_high python -B -m py_compile \
  backend/main.py backend/renderer.py backend/ai.py backend/config.py \
  backend/llm_adapters.py backend/job_store.py backend/quality_gate.py \
  backend/security.py backend/copy_policy.py backend/library.py \
  backend/drawing_converter.py backend/plates.py streamlit_app.py
conda run -n sprint_high python tools/scan_secrets.py --all

bash start.sh --restart
curl -s http://127.0.0.1:8010/health | head -c 300

# 5) 머지 커밋
git commit -m "merge: webdemo (모델링 1차) into taeho - 보안/LLM/ComfyUI/quality 통합"
git push origin taeho
```

### 0-4. 머지 후 확인할 회귀 항목

- [ ] `/health` 200, `redacted_settings()` 모든 secret 마스킹
- [ ] `/ai/providers` 6개 (kanana/midm/local/openai/hyperclova/fallback)
- [ ] `/render/desk-setup` (taeho) vs `/render/keyboard-preview` (webdemo) 둘 다 200
- [ ] `/ai/image/jobs` queue → ComfyUI poll → 합성 완료
- [ ] layout 60/65/75 + 신규 87/104 모두 응답
- [ ] streamlit 단계별 UI 모두 표시 + 충돌 영역 없음
- [ ] `tools/scan_secrets.py --all` clean

### 0-5. 머지 의사 결정 회의 항목

webdemo가 어떤 의도로 만들어졌는지 같이 협업자와 확인 필요. 다음 중 어느 쪽이 정책인지 회의 한 번:

1. `webdemo`가 발표/데모용 별도 흐름이고 `taeho`는 개발 누적이라면 → 일단 머지 보류, 별도 운영
2. `webdemo`가 새 모델링 기준이라면 → 이 머지가 base가 되고 이후 작업은 webdemo 위에서
3. 둘 다 main으로 합칠 거라면 → 본 문서 §0-3 절차로 일괄 머지

---

## 1. 모델 연결 작업 (Model Integration)

### 1-A. 즉시 가능 (덜 위험)

| # | 작업 | 상세 | 효과 |
|---|---|---|---|
| M1 | ComfyUI systemd unit | **완료**: `/etc/systemd/system/comfyui.service` 등록, 재부팅 자동 기동, `journalctl -u comfyui` 로그 표준화, `start.sh` ComfyUI/Ollama 의존성 체크 추가 | 운영 안정성 확보 |
| M2 | image job 결과 재사용 갤러리 | 현재 `/ai/image/jobs` 목록 API는 있지만 UI에서 1건만 노출. 같은 prompt로 N장 batch 생성 후 사용자가 picking | 발표/데모 시 베스트 컷 선택 가능 |
| M3 | `/ai/image/jobs/{id}/regenerate` | 동일 payload + 새 seed로 변주 생성 endpoint | 사용자가 "마음에 안 들면 다시" 한 번 클릭 |
| M4 | 모델 hot-swap | `OLLAMA_MODEL`을 다양화 (qwen2.5:7b, gemma2:9b, llama-3.1-korean 등) 후 `/ai/providers`에 선택지 노출 | 모델별 카피 톤 차이 데모 가능 |

### 1-B. 중간 작업 (모델 가중치/스택 필요)

| # | 작업 | 상세 | 의존 |
|---|---|---|---|
| M5 | 실 Kanana / Mi:dm 가중치 분리 운영 | 현재는 두 슬롯 모두 qwen2.5:7b를 가리킴. 실 가중치를 vLLM 또는 SGLang으로 다른 포트(11436/11437)에 올리고 `.env` 슬롯 분리 | HF 토큰 + L4 24GB 메모리 스케줄링 |
| M6 | ControlNet (canny / depth) 워크플로우 | 현재 FLUX schnell 기본 텍스트→이미지. 도면 라인을 ControlNet canny로 강제해 제품 보존 | ControlNet 가중치 다운로드 + workflow JSON 작성 |
| M7 | IP-Adapter / FaceID 스타일 | 사용자가 자기 셋업 사진을 reference로 주면 그 톤으로 합성 | IP-Adapter 가중치 |
| M8 | LoRA 슬롯 | 점주별 LoRA 학습/적용 인프라. `models/loras/` 폴더 + workflow에서 동적 로드 | DreamBooth/SDXL LoRA 학습 파이프라인 |

### 1-C. 큰 작업 (별도 GPU/스토리지 필요)

| # | 작업 | 상세 | 비고 |
|---|---|---|---|
| M9 | FLUX dev 또는 SDXL 추가 | schnell은 4-step 빠른 모델. 더 디테일 필요한 경우 dev(20-30 step) | VRAM 빡빡, 양자화 필수 |
| M10 | OCR + Canny IoU 품질 워커 | `backend/quality_gate.py`의 placeholder를 실제 워커로 확장 | paddleocr 또는 easyocr + opencv |
| M11 | HyperCLOVA direct API 실 호출 | 현재 `HyperClovaDirectAdapter`는 코드만 있고 회귀 테스트 없음. Naver Cloud 키 받으면 통합 테스트 추가 | 사내 결제/계정 |

---

## 2. UI 향상 작업 (Frontend UX)

`webdemo`가 갖고 있을 도면 UI와 충돌하지 않도록 §0 머지 후 진행 권장.

### 2-A. 단순 개선 (시간 적음)

| # | 작업 | 상세 | 효과 |
|---|---|---|---|
| U1 | 4단계 진행 stepper 시각화 | 현재 selectbox만으로 단계 이동. `st.progress` + 단계명 chip | 데모 시 흐름이 한눈에 |
| U2 | 광고 카피 카드 정리 | `한글 모델 비교 결과` expander를 4-card grid로 (제공자별 columns) | 비교 한 화면 |
| U3 | 포스터 템플릿 썸네일 | 4개 템플릿(`minimal_card`, `grid_three`, `feature_focus`, `promo_banner`) 미리보기 SVG inline 표시 | 선택 전 결정 도움 |
| U4 | 이미지 작업 history grid | `/ai/image/jobs?limit=20` 결과를 썸네일 grid로 표시, 클릭하면 포스터 합성에 재사용 | 데모/발표 시 베스트 컷 |
| U5 | 한국어 UI 라벨 일관성 | 현재 영문/한글 혼재. 모든 UI 라벨 한글로 통일 (개발 단계 표기는 예외) | 사용자 친화 |

### 2-B. 흐름 개선 (중간 규모)

| # | 작업 | 상세 |
|---|---|---|
| U6 | 3D viewer camera preset 버튼 | 현재 camera-orbit URL 파라미터로 수동. perspective/top/front 3개 버튼 |
| U7 | "AI 추천 셋업" 원클릭 | 상품 정보만 입력 → 책상 크기/모니터/에셋/색상까지 자동 추천 후 결과 즉시 표시 |
| U8 | 광고 카피 채널 동시 생성 | 한 번 입력 → 인스타그램/스마트스토어/쿠팡 채널별 카피 3종 동시 생성 (정책별 길이 자동 적용) |
| U9 | 포스터 다운로드 형식 다양화 | 현재 SVG만. PNG 1080/1350/1600 export 버튼 추가 (cairosvg 또는 wand) |
| U10 | 이미지 품질 리포트 시각화 | `/ai/image/jobs/{id}/quality` 결과를 chart로 (해상도, aspect ratio, bytes, OCR 결과 미래) |

### 2-C. 큰 변경 (구조 설계 필요)

| # | 작업 | 상세 |
|---|---|---|
| U11 | 즐겨찾기 / 갤러리 영속화 | 사용자가 자기 product/poster를 저장/불러오기. job_store와 유사한 패턴으로 `data/runtime/favorites.jsonl` |
| U12 | 다국어 (영/일) UI | i18n 분리 — 한국어 위주이지만 데모용 영어 캡션 옵션 |
| U13 | 모바일 반응형 | Streamlit 자체는 반응형이지만 우리 CSS(`block-container max-width: 1680px`)가 모바일에서 끊김. media query 추가 |
| U14 | 다크 모드 | streamlit theme + 우리 palette 다크 변형 (현재 4개 theme이 라이트 톤만) |

---

## 3. 작업 의존성 / 권장 순서

```text
P0   webdemo 머지 (§0)                  ← 모든 작업의 base
 ├─→ M1 ComfyUI systemd                 (운영 안정화, 누구든 가능)
 ├─→ U1, U2, U3, U5                     (UI 시각 개선, 머지 직후)
 ├─→ U4 ← M2, M3                        (이미지 갤러리 + batch/regenerate)
 ├─→ M6 ControlNet ← M1                 (모델 안정화 후 도면 보존 합성)
 ├─→ U8                                  (채널별 카피 동시 생성)
 ├─→ U9                                  (포스터 PNG export)
 └─→ M10 OCR 품질 워커                  (스케줄러/별도 worker process)
```

발표/데모 우선이라면: **P0 → M1 → U1 → U3 → U4 → U2** 순서가 시각적 인상이 가장 빠르게 향상된다.

---

## 4. 미해결 / 관찰만 하기

다음 항목들은 지금 손대지 말고 별도 회의 후 결정:

- LoRA 학습 파이프라인 (시간/비용 크고, GPU 점유 큼)
- HyperCLOVA direct API (Naver Cloud 결제 필요)
- multi-GPU 분산 추론 (현재 L4 1장)
- ComfyUI 자체 보안 (8188이 loopback이지만 plugin 자체 vuln 가능성)
- 사용자 인증을 nginx basic auth 너머의 OAuth/OIDC로 (현재 데모 단계에서는 과함)

---

## 5. 검증 명령 한 줄 (병합 후 매번 실행)

```bash
cd /home/leetaeho/ai_07_high/deskad_keyboard_demo && \
conda run -n sprint_high python -B -m py_compile \
  backend/*.py streamlit_app.py tools/scan_secrets.py && \
conda run -n sprint_high python tools/scan_secrets.py --all && \
bash start.sh --restart && \
curl -sk -o /dev/null -w "nginx: %{http_code}\n" https://127.0.0.1:8443/ && \
curl -s  -o /dev/null -w "fastapi: %{http_code}\n" http://127.0.0.1:8010/health
```

기대 출력: `clean`, `nginx: 401`, `fastapi: 200`.
