# 2026-06-04 Report

## 작업 요약

- `origin/main`에 새 커밋이 있어 `git pull --ff-only --autostash origin main`으로 최신 main을 받았다.
- pull 자체는 fast-forward로 성공했지만, autostash 재적용 중 `streamlit_app.py`, `backend/runtime_workers.py`에서 충돌이 발생했다.
- 충돌은 원격 기능과 로컬 UI 모듈화 작업을 모두 살리는 방향으로 정리했다.
- 원격의 `ui_api.py`와 로컬의 `ui/api_client.py`가 API helper 역할을 중복으로 들고 있어 구현 위치를 `ui/api_client.py`로 통합했다.
- `ui_api.py`는 기존 import 호환을 위한 re-export wrapper로 정리했다.
- `streamlit_app.py` 상단의 큰 기본 CSS `st.markdown()` 블록을 `ui/styles.py`의 `render_base_layout_styles()`로 분리했다.

## Pull로 들어온 주요 변경

- ComfyUI 테마 워크플로 3종 추가:
  - `deskad_keyboard_demo/tools/comfyui_workflows/flux_pastel.json`
  - `deskad_keyboard_demo/tools/comfyui_workflows/flux_gaming.json`
  - `deskad_keyboard_demo/tools/comfyui_workflows/flux_premium.json`
- ComfyUI workflow selector, negative prompt, LoRA config 관련 backend/UI 변경이 들어왔다.
- 결과 캐시 TTL/LRU eviction 관련 변경이 들어왔다.
- 레퍼런스 썸네일 grid, 포스터 미리보기 높이 계산 helper가 들어왔다.
- 보안 문서와 테스트가 추가/보강됐다.

## 충돌 해결 기준

- `backend/runtime_workers.py`
  - 원격의 lock 파일 권한 `0600` 처리 유지.
  - 로컬의 Windows `fcntl is None` 안전 처리 유지.
- `streamlit_app.py`
  - 원격의 레퍼런스 썸네일 grid와 포스터 iframe 높이 계산 유지.
  - 로컬의 테마 모드, 스타일 분리, 상품 입력 좌우 레이아웃 유지.
  - 판매 채널 목록은 원격에서 늘어난 항목까지 포함했다.

## 추가 모듈화

- `deskad_keyboard_demo/backend/schemas.py`
  - FastAPI request Pydantic 모델들을 `backend/main.py`에서 분리했다.
  - 이동한 모델:
    - `KeyboardRenderRequest`
    - `DeskSetupRenderRequest`
    - `SelectedCopy`
    - `AdContentRequest`
    - `UploadedModelRequest`
    - `LibraryModelRequest`
    - `CopyExperimentRequest`
    - `PlateDrawingRenderRequest`
  - `main.py`는 schemas import만 사용하게 바꿨고, Pydantic 직접 import를 제거했다.
  - `main.py` 줄 수는 480줄로 줄었다.
- `deskad_keyboard_demo/backend/errors.py`
  - FastAPI `HTTPException` 생성 helper를 분리했다.
  - 추가한 helper:
    - `bad_request()`
    - `not_found()`
    - `server_error()`
  - `backend/main.py`는 `HTTPException`을 직접 import하지 않고 위 helper를 사용한다.
- health/security 보안 확인
  - `/health`, `/security/config`는 앱 상태와 보안 상태 확인에 가까워 `backend/main.py`에 유지하기로 했다.
  - `/security/config`는 기존처럼 `redacted_settings()`만 반환한다.
  - `/health`는 기존 응답 형태를 유지해 `{"status": "ok", "config": redacted_settings()}`를 반환한다.
  - `redacted_settings()`에서 `openai_api_key`, `hyperclova_api_key`, `kanana_api_key`, `midm_api_key`는 모두 `mask_value()`를 거치는 것을 확인했다.
  - 현재 단계에서 원문 secret을 추가로 노출하는 경로는 발견하지 못했다.
- `deskad_keyboard_demo/backend/routes/`
  - FastAPI route 분리를 다시 시작했다.
  - `/health`, `/security/config`는 `main.py`에 유지하고, 기능 도메인이 분명한 route부터 분리했다.
  - `routes/__init__.py`에 `register_routes(app)`를 추가했다.
  - `routes/assets.py`로 다음 endpoint를 이동했다:
    - `/assets/desk`
    - `/assets/references`
    - `/models/library`
    - `/layouts`
  - `routes/plates.py`로 다음 endpoint를 이동했다:
    - `/plates`
    - `/plates/brands`
    - `/plates/{plate_id}/preview`
  - `backend/main.py`는 `register_routes(app)` 호출로 assets/plates 라우터를 등록한다.
  - `backend/main.py` 줄 수는 420줄로 줄었다.
- `deskad_keyboard_demo/ui/api_client.py`
  - 기존 API/cache helper에 원격에서 들어온 asset helper를 합쳤다.
  - 추가 통합 항목:
    - `reference_thumbnail_bytes()`
    - `svg_aspect_ratio()`
    - `poster_preview_height()`
    - `responsive_svg_document()`
- `deskad_keyboard_demo/ui_api.py`
  - 실제 구현을 제거하고 `ui.api_client` re-export wrapper로 변경했다.
  - 기존 코드가 `ui_api`를 import해도 동작하도록 호환성을 남겼다.
- `deskad_keyboard_demo/ui/styles.py`
  - `render_base_layout_styles()`를 추가했다.
  - `streamlit_app.py` 상단의 큰 기본 CSS `st.markdown()` 블록을 이 함수로 이동했다.
- `deskad_keyboard_demo/streamlit_app.py`
  - 기본 CSS는 `render_base_layout_styles()` 호출로 대체했다.
  - 화면 모드 CSS는 기존처럼 `render_ui_theme_styles()` 호출로 유지했다.
  - API/helper import는 `ui.api_client` 중심으로 정리했다.
- `deskad_keyboard_demo/ui/constants.py`
  - Streamlit 화면에서 쓰는 순수 상수와 label dictionary를 분리했다.
  - 이동한 주요 항목:
    - `STEP_LABELS`
    - `CASE_FINISH_LABELS`
    - `PLATE_MATERIAL_LABELS`
    - `PCB_COLOR_LABELS`
    - `SWITCH_STEM_LABELS`
    - `SWITCH_FAMILY_LABELS`
    - `KEYCAP_PROFILE_LABELS`
    - `MOUNT_TYPE_LABELS`
    - `MONITOR_ARM_LABELS`
    - `POSTER_TEMPLATE_LABELS`
    - `PROVIDER_LABELS`
    - `POSTER_TEMPLATE_THUMBNAILS`
    - `MONITOR_SIZES`
    - `KEYBOARD_SIZE_INFO`
    - `IMAGE_JOB_TERMINAL_STATUSES`
    - `KEYBOARD_MODEL_DEFAULTS`
- `deskad_keyboard_demo/ui/defaults.py`
  - Streamlit session 기본값 `DEFAULTS`를 분리했다.
  - `ui_theme_mode` 기본값은 기존처럼 `DEFAULT_THEME_MODE`를 사용한다.
- `streamlit_app.py` 줄 수는 1378줄에서 1143줄로 줄었다.

## 검증

- 다음 문법 검사를 통과했다.

```powershell
python -m py_compile deskad_keyboard_demo\streamlit_app.py deskad_keyboard_demo\ui\constants.py deskad_keyboard_demo\ui\defaults.py deskad_keyboard_demo\ui\styles.py deskad_keyboard_demo\ui\api_client.py deskad_keyboard_demo\ui\theme.py deskad_keyboard_demo\ui_api.py deskad_keyboard_demo\backend\runtime_workers.py deskad_keyboard_demo\backend\main.py deskad_keyboard_demo\backend\schemas.py deskad_keyboard_demo\backend\errors.py deskad_keyboard_demo\backend\routes\__init__.py deskad_keyboard_demo\backend\routes\assets.py deskad_keyboard_demo\backend\routes\plates.py deskad_keyboard_demo\backend\app_factory.py
```

- 문법 검사 중 갱신된 추적 `__pycache__` 파일은 작업 산출물이 아니므로 원래 상태로 되돌렸다.

## 남은 정리 후보

- `streamlit_app.py`의 `render_step_progress()`, `render_campaign_studio_header()`, `render_studio_status_cards()`를 `ui/components.py`로 분리.
- 광고 카드/생성 문구 미리보기는 `ui/ad_preview.py`로 분리.
- session 기본값과 라벨 dictionary는 `ui/defaults.py` 또는 `ui/constants.py`로 분리.

## 2026-06-04 추가 pull 및 충돌 해결

- `git pull --ff-only --autostash origin main`으로 `main`을 `39227fb`까지 갱신했다.
- 새로 들어온 주요 upstream 변경:
  - `ui_steps.py` 입력 패널 분리
  - ComfyUI VRAM 자동 언로드 흐름
  - 워크플로 selector 확장과 `image_workflow` 요청 필드
  - 파스텔/다크 적응형 UI, 포스터 SVG 정비, provider 표시 개선
- autostash 재적용 중 충돌이 발생한 파일:
  - `deskad_keyboard_demo/backend/main.py`
  - `deskad_keyboard_demo/streamlit_app.py`
  - `deskad_keyboard_demo/ui_api.py`
- 충돌 해결 기준:
  - `backend/main.py`는 기존에 분리한 `app_factory`, `routes`, `schemas`, `errors` 구조를 유지했다.
  - `/health`, `/security/config`는 기존 논의대로 `backend/main.py`에 남겼다.
  - upstream의 `image_workflow` 검증 필드는 `backend/schemas.py`의 `AdContentRequest`로 반영했다.
  - `streamlit_app.py`는 upstream의 `ui_steps.py` 기반 입력 패널 분리를 채택하고, 기존 `ui.constants`, `ui.defaults`, `ui.styles`, `ui.theme`, `ui.api_client` 분리 구조와 같이 사용하도록 합쳤다.
  - `ui_api.py`는 실제 구현을 다시 넣지 않고 `ui.api_client` re-export 호환 래퍼로 유지했다.
  - upstream의 SVG `viewBox` 파싱 개선은 `ui/api_client.py`의 `svg_aspect_ratio()`에 반영했다.
- 추가 연결:
  - `ui/defaults.py`에 `image_workflow` 기본값을 추가했다.
  - `streamlit_app.py`의 광고 payload에 `image_workflow`를 포함했다.
- 검증:
  - 충돌 마커 검색에서 `<<<<<<<`, `=======`, `>>>>>>>`가 남지 않은 것을 확인했다.
  - `python -m py_compile`로 백엔드/프론트 분리 파일과 `ui_steps.py` 문법 검사를 통과했다.
## 2026-06-04 추가 pull 2차 및 충돌 해결

- `git pull --ff-only --autostash origin main`으로 `main`을 `c6d51a4`까지 갱신했다.
- 새로 들어온 주요 upstream 변경:
  - `feat/copy-edit-ppt-export` 병합
  - 카피 인라인 편집 기능
  - 포스터 PPTX 다운로드 기능
  - `ppt_export.py` 신규 추가
  - `python-pptx` 의존성 추가
  - 포스터 CTA 동적 폭 테스트와 PPT export 테스트 추가
  - 이미지 프롬프트 구도/키보드 도메인 최적화 PR 병합
- autostash 재적용 중 `deskad_keyboard_demo/streamlit_app.py`에서 충돌이 발생했다.
- 충돌 해결 기준:
  - 기존 분리 방향에 맞춰 API helper import는 `ui.api_client`를 유지했다.
  - upstream의 `build_poster_pptx` import와 PPTX 다운로드 버튼은 유지했다.
  - 중복으로 들어온 광고 카드 미리보기 블록은 제거했다.
  - 대신 기존 `render_ad_card_preview_section()` 안에 upstream의 `render_copy_inline_editor()`를 연결해 카피 편집 기능을 살렸다.
- 검증:
  - 충돌 마커 검색에서 `<<<<<<<`, `=======`, `>>>>>>>`가 남지 않은 것을 확인했다.
  - `python -m py_compile`로 `streamlit_app.py`, `ppt_export.py`, 주요 분리 모듈 문법 검사를 통과했다.
  - `git diff --check` 형식 검사를 통과했다.
## 2026-06-04 로컬 수정 재적용 확인

- 추가 pull 이후 autostash에 남은 변경과 현재 staged 변경을 비교했다.
- 기존에 우리가 수정한 주요 작업이 현재 작업트리에 다시 반영된 것을 확인했다.
  - `backend/main.py`의 `app_factory`, `routes`, `schemas`, `errors` 분리 구조
  - `ui_api.py`의 `ui.api_client` re-export wrapper 구조
  - `streamlit_app.py`의 `ui.constants`, `ui.defaults`, `ui.styles`, `ui.theme`, `ui.api_client` 분리 import
  - `image_workflow` payload/default/schema 연결
  - upstream의 `build_poster_pptx`와 카피 편집 기능 유지
- `ui/styles.py`의 trailing whitespace를 정리했다.
- 검증:
  - 주요 한글 문자열이 UTF-8 대체문자 없이 존재하는지 확인했다.
  - `python -m py_compile` 문법 검사를 통과했다.
  - `git diff --check` 형식 검사를 통과했다.